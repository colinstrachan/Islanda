<%= render "components/banner" %>
<div class="container">
<h3 style="text-align: center"> Where do you want to go? </h3>
<%= render "maps" %>
  <div class="row">
    <% @islands.each do |i| %>
    <div class="col-xs-12 col-sm-4">
     <%= link_to island_path(i) do %>
     <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('<%= cl_image_path i.photo, crop: :fill %>')">
      <div class="card-category">
        <%= i.name %>
      </div>
      <div class="card-description">
        <h2><%= i.location.capitalize %></h2>
        <p><%= i.description %></p>
      </div>
    </div>
    <% end %>
  </div>
  <% end %>
</div>
